void generate(){
  for(int i = 0; i<20; i++){ //symuluję opoznienie
      addProgress(5); //uznaniowo wykonałem 5% generacji mapy, więc wysyłam info
      delay(500); //symuluję jakieś Twoje operacje
  }
}
